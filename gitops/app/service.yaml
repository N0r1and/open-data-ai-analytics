apiVersion: v1
kind: Service
metadata:
  name: housing-web
  namespace: housing
spec:
  selector:
    app: housing-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
